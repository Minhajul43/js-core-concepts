console.log(2 === 2);
console.log(2 === 3);
console.log(2 === '2');

// three equals compare the data type also!
// for example 2 and '2' are different data type so they are not considered equal when we use triple equals! but when we use double equals they are considered equal because they have the same value!